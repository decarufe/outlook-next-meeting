# Squad: ignore runtime state (logs, inbox, sessions)
# Note: .squad/casting/* is identity and MUST be committed to main, not ignored
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
.squad/.cache/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# .NET build outputs
bin/
obj/
.vs/
*.user

# Local MSIX signing material (never commit real dev certs or private keys)
*.pfx
*.cer
*.pvk
src/OutlookNextEvent.App/Package.local.props
src/OutlookNextEvent.App/MsixPackages/
